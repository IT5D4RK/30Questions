// Method 1
const shirtMsg = `JDM4LIFE`;
const shirtSize = `large`;

function makeShirt() {
    console.log(`The shirt is going to be ${shirtSize} in size and have this message printed on it: ${shirtMsg}`);
}

makeShirt();

// Method 2
function makeShirt2(shirtSize2, shirtMsg2) {
    console.log(`The shirt size is ${shirtSize2} and it has the following message printed on it: ${shirtMsg2}`);
  }
  
makeShirt2("medium", "<Programmer/>");
  